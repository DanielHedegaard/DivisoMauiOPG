<?xml version="1.0" encoding="utf-8" ?>
<mct:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DivisoMauiOPG.Views.Popups.DeletePopup"
             xmlns:mct="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
           Size="300, 150">
    <VerticalStackLayout>
        <Label 
            Text="Vil du slette denne addresse fra din gemte liste?"
            VerticalOptions="Center" 
            HorizontalOptions="Center" 
            Grid.Column="2"
            Padding="10"
            TextColor="Black"/>
        <HorizontalStackLayout HorizontalOptions="Center">
            <Button x:Name="yesBtn" Grid.Row="1" Grid.Column="0" Text="Ja" Clicked="yesBtn_Clicked"/>
            <Button x:Name="noBtn" Grid.Row="1" Grid.Column="2" Text="Nej" Clicked="noBtn_Clicked"/>
        </HorizontalStackLayout>
    </VerticalStackLayout>
</mct:Popup>